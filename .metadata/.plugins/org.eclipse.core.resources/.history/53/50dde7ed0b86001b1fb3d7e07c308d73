import mysql.connector

mydb = mysql.connector.connect(
    host="localhost",
    user="root",
    password="python",
    database="python"
)

mycursor = mydb.cursor()

sql = "select * from sample"

mycursor.execute(sql, val)

mydb.commit()

print(mycursor.rowcount, "record was selected")
